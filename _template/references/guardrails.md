# <skill-name> · 红线与失败兜底

> 每阶段动手前必看。SKILL.md 里只放速记，完整红线放这里。

## 红线（不可逾越）

1. <列出本 skill 的硬约束，例如「未确认需求不开工」>
2. **不静默降级** — 解析失败 / 工具不可用 / 缺 token → 🔴 STOP + 给选项。
3. **不替用户假设路径** — 找不到就问，不猜。
4. **自愈有上限** — 同一问题修不过 N 次 → 停 + 上报。

## 失败兜底速查表

| 失败场景 | 处理 |
|---------|------|
| <工具未装> | <提示用户跑 init，或 agent 自动装> |
| <MCP 不可用> | <STOP + 给降级选项> |
| <路径找不到> | <AskUserQuestion 问用户> |
