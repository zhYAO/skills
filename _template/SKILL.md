---
name: template-scaffold-placeholder
description: 【这不是一个可用的 skill，是新增 skill 的脚手架模板，请勿安装或索引】复制本目录改名后再用（见同目录 README.md）。下面这段是填写指引——真实 description 应是自然语言触发器：一句话说清「做什么」+「什么场景触发」+「触发词举例」。多数 skill 凭这个字段自动匹配加载，写得越具体越不会误触发/漏触发。如果是代价大、易误触发的流水线 skill，加 disable-model-invocation: true 并删掉本句对"自动加载"的描述。
# 主动调用型 skill 才加下面这行（删则为自然语言自动触发）：
# disable-model-invocation: true
allowed-tools: Read Edit Write Glob Grep Bash
---

# <skill-name> · 工作流

<一句话说明这个 skill 解决什么问题、为什么值得固化成流程。>

---

## 🛑 Cardinal rules（动手前必看）

1. <最容易翻车的红线，例如「未确认需求不开工」>
2. **不静默降级** — 解析失败 / 工具不可用 / 缺 token → 直接 🔴 STOP + 给选项，不空跑、不硬产报告。
3. **不替用户假设路径** — 路径 / 命令 / 目标端找不到就问，不猜。
4. <自愈/重试有上限，到限上报，不无限重试也不强标通过。>

---

## 🗂 Step 0 · 任务类型路由（如适用 · 否则删本节）

进入主流程前先识别任务类型，按类型产对应文档，不一律套同一套。

| 类型 | 触发词举例 | 文档清单 |
|------|-----------|----------|
| <类型 A> | <触发词> | <产出文档> |
| <类型 B> | <触发词> | <产出文档> |

不确定 → `AskUserQuestion`。

---

## 🔁 阶段表

开始前用 `TaskCreate` 把各阶段登记成任务，逐阶段标完成。

| 阶段 | 关键动作 | 产物 / 子 skill | 详情 |
|------|----------|----------------|------|
| 1 · <名> | <动作> | <产物> | `references/<x>.md` |
| 2 · <名> | <动作> | <产物> → 🔴 **卡点**（如需人工确认） | `references/<x>.md` |

---

## 🔄 关键卡点（如适用）

- **🔴 卡点 1 · <在哪一步>** — <未拿到用户什么明确同意前禁止下一步>。

---

## 📦 内部子 skill（如适用 · 否则删本节）

把「上下文重 / 需要独立视角」的活下沉到 `context: fork` 子 skill（见 `skills/<sub>/SKILL.md`），
海量日志 / 独立审查挡在主上下文外。子 skill 接收**目标项目根的绝对路径**作为参数。

---

## 📁 文件地图

| 目录 | 用途 |
|------|------|
| `assets/*.template.md` | 产出物模板 |
| `references/guardrails.md` | 红线 + 失败兜底，每阶段动手前必看 |
| `references/*.md` | 各阶段详细工作流（按需加载） |
| `scripts/*.sh` | 环境检测 / 安装脚本（检测 → 征得同意 → 自动装，幂等） |
| `commands/*.md` | Claude Code 斜杠命令（可选） |
| `INSTALL.md` | 跨 agent 安装指引 |
